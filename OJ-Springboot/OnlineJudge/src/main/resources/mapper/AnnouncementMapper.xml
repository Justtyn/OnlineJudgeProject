<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.onlinejudge.mapper.AnnouncementMapper">
    <select id="selectTypeDistribution" resultType="java.util.Map">
        SELECT type, COUNT(*) as count
        FROM announcement
        GROUP BY type
    </select>

    <select id="selectTimeDistribution" resultType="java.util.Map">
        SELECT DATE_FORMAT(create_time, '%Y-%m') as month, COUNT(*) as count
        FROM announcement
        GROUP BY DATE_FORMAT(create_time, '%Y-%m')
        ORDER BY month DESC
        LIMIT 12
    </select>
</mapper> 